const raio = 5.6
const area = Math.PI * Math.pow(raio, (2).toFixed(2))
/* o Math é um objeto 
que tem uma constante PI
Math.pow é uma função do objeto Math
*/
console.log(area)
console.log(typeof Math)